name: CI Check
on:
  push:
    paths-ignore:
      - 'CHANGELOG.md'
jobs:
  check_code:
    name: Check Code
    runs-on: self-hosted
    steps:
      - name: Check out the repo
        uses: actions/checkout@v2
        with:
          fetch-depth: 0